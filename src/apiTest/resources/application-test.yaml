spring: 
  datasource: 
    url: jdbc:postgresql://172.17.0.2:5432/algafood_test?serverTimezone=UTC
    username: postgres
    password:  
    data: classpath:data.sql
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    baselineOnMigrate: true
